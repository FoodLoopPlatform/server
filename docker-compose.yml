version: "3.9"

services:
  foodloop-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: foodloop-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080